<!DOCTYPE html>
<html>
<head>
  <title>Reveal Secret</title>
  <link rel="stylesheet" href="/styles.css">
  <style>
    body{
      height: 100vh;
      width: 100vw;
      overflow: hidden;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      background-color: rgb(255, 227, 231);
    }
    .container{
      display: flex;
      flex-direction: column;
      justify-content: center;
      
    }
    form{
      width: 100%;
      height: 100%;
      
    }
    input{
      width: 99%;
      height: 40%;
      color: black;
      font-weight: 700;
      border-radius: 5px;
      font-size: large;
    }
    button{
      background-color: green;
      line-height: 30px;
      width: 100px;
      align-items: center;
      border: none;
      border-radius: 5px;
      color: antiquewhite;
      height: 40px;
      margin-left: 35%;
      font-size: larger;
      font-family: 'Times New Roman', Times, serif;
    }
    
  </style>
</head>
<body>
  <div class="container">
  <h1>Enter Details to Reveal the Message 💌</h1>
  <% if (error) { %>
    <p class="sec-msg" style="color:red;"><%= error %></p>
  <% } %>
  
  <form action="/reveal" method="POST">
    <input type="hidden" name="id" value="<%= id %>">
    <input type="text" name="from" placeholder="Your Name" required><br><br>
    <input type="text" name="to" placeholder="Passkey" required><br><br>
    <button type="submit">Reveal</button>
  </form>
  </div>
</body>
</html>
