<!DOCTYPE html>
<html>

<head>
    <title>Your Secret QR</title>
    <link rel="stylesheet" href="/styles.css">
    <style>
        body {
            width: 100vw;
            height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            overflow: hidden;
        }
        .container{
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }
        .qr-img{
            height: 300px;
            width: 300px;
        }
        button{
            background-color: orange;
            border: none;
            height: 50px;
            width: 150px;
            line-height: 40px;
            font-size: large;
            font-family: 'Times New Roman', Times, serif;
            border-radius: 10px;
        }
    </style>
</head>

<body>
    <div class="container">

        <h1>Your QR Code is Ready 🎉</h1>
        <img src="<%= qrImage %>" alt="QR Code" class="qr-img"><br>
        <a download="rakhi-secret-qr.png" href="<%= qrImage %>"><button>Download QR</button></a>
    </div>
</body>

</html>